<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Flappy_Miau.ViewModels"
    x:Class="Flappy_Miau.Views.MainMenuPage"
    x:DataType="viewModels:MainMenuViewModel"
    Title="{Binding MainMenu}">
    <ScrollView>
        <VerticalStackLayout
            WidthRequest="200"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Spacing="10">
            <Button
                Text="{Binding NewGameButton}"
                Command="{Binding NewGameCommand}"
                HorizontalOptions="Fill" />
            <Button
                Text="{Binding HighScoreButton}"
                Command="{Binding HighScoreCommand}"
                HorizontalOptions="Fill" />
            <Button
                Text="{Binding ExitGameButton}"
                Command="{Binding ExitGameCommand}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>